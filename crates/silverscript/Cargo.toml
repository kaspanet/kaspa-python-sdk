[package]
name = "kaspa-python-sdk-silverscript"
version.workspace = true
edition.workspace = true

[lib]
# Compiled to `kaspa/experimental/silverscript/silverscript.<abi>.so` and re-exported by
# `kaspa/experimental/silverscript/__init__.py` as `from .silverscript import *`.
name = "silverscript"
crate-type = ["cdylib", "rlib"]

[[bin]]
name = "stub-gen"
path = "src/bin/stub_gen.rs"

[features]
extension-module = ["pyo3/extension-module"]

[dependencies]
kaspa-python-sdk-core = { path = "../core" }
pyo3 = { version = "0.27.1" }
pyo3-stub-gen = "0.17.2"
self_cell = "1"
silverscript-lang = { git = "https://github.com/kaspanet/silverscript.git", rev = "2a3961cadc76bb16a425042172ffe32481da89b5" }
# The debug harness executes spends through the same rusty-kaspa revision the
# silverscript workspace pins (tag v2.0.1) — distinct from the core module's rev.
debugger-session = { git = "https://github.com/kaspanet/silverscript.git", rev = "2a3961cadc76bb16a425042172ffe32481da89b5" }
kaspa-consensus-core = { git = "https://github.com/kaspanet/rusty-kaspa", tag = "v2.0.1" }
kaspa-txscript = { git = "https://github.com/kaspanet/rusty-kaspa", tag = "v2.0.1" }
